package org.example.chess.pieces;

import org.example.chess.pieces.global.Position;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;

public class PawnTest {
    @Test
    public void Pawn에게_주어진_이동이_유효하다() {
        Pawn pawn = Pawn.of(Piece.Color.WHITE);

//        Assertions.assertTrue(pawn.isMovable(Position.of("b1"), Position.of("b2")));
//        Assertions.assertTrue(pawn.isMovable(Position.of("b1"), Position.of("a1")));
//        Assertions.assertTrue(pawn.isMovable(Position.of("b1"), Position.of("b0")));
//        Assertions.assertTrue(pawn.isMovable(Position.of("b1"), Position.of("c1")));

    }
}
